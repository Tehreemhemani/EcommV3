const initialState = {
    UserInfo:{
        name: "Tehreem Fatima",
        designation: "React native Developer",
        age:"32"
    }
}

export default(state=initialState,action) =>{
    return state
}